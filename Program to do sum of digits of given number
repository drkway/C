// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int n,sum=0,rem;
    printf("Enter The Number:");
    scanf("%d",&n);
    while(n>0){
        rem=n%10;
        sum=sum+rem;
        n=n/10;
    }
    printf("The Sum is %d",sum);
    return 0;
}
