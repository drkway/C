// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int x=2,num;
    printf("Enter Number's To:");
    scanf("%d",&num);
    while(x<=num){
        printf("%d\n",x);
        x=x+2;
    }
    return 0;
}
